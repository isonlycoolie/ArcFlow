# Transitive: arcflow-core -> qdrant-client -> tonic -> rustls-pemfile (unmaintained, no direct fix).
# Transitive: arcflow-core -> sqlx -> rsa (Marvin timing advisory; no fixed upgrade).
[advisories]
ignore = ["RUSTSEC-2025-0134", "RUSTSEC-2023-0071"]
